<article class="card mb-2">
    <div class="card-body">
        <p>{{item.origin}}</p>
        <p>{{item.shortURL}}</p>
        <a href="/home/eliminar/{{item._id}}" class="btn btn-danger">Eliminar</a>
        <a href="/home/editar/{{item._id}}" class="btn btn-warning">Editar</a>
        <button data-short="{{item.shortURL}}" href="#" class="btn btn-info">Copiar</button>
    </div>
</article>